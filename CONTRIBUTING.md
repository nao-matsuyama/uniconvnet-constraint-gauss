Contributing

- Use `dev` branch for active development and open pull requests against `main`.
- Run pre-commit hooks locally: `pre-commit run --all-files`.
- Large model files (`*.pth`) should be added via Git LFS: `git lfs track "*.pth"`
